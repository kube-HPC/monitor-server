{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/app.js",
            "outputCapture": "std",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "envFile": "${env:HOME}/dev/hkube/.env"
        }
    ]
}